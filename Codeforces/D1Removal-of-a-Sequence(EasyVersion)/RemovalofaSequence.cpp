#include <stdio.h>


int main() {

    int t; 

    scanf("%d", &t);

    for (int i =0 ; i < t; i++) {
        int x, y, k;
        scanf("%d %d %d", &x, &y, &k);

        for (int j = 0; j < )
    }


    return 0;
}